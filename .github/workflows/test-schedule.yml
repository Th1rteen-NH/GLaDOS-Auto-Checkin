name: Test Schedule

on:
  schedule:
    - cron: '*/1 * * * *'   # 每5分钟一次，仅用于测试
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Schedule triggered at $(date)"
