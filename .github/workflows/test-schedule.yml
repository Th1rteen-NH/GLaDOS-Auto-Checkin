name: Test Schedule

on:
  schedule:
    - cron: '*/5 8 * * *'   # 每5分钟一次，仅用于测试
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Schedule triggered at $(date)"
